server:
  port: 9292
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/customer_info_db
    username:
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true